#include "problem_lx.h"
#include "problem_st.h"

struct tree_t lex2tree(struct lexem_t lex) {
    struct tree_t top;

    switch (lex.kind) {
        case OP:
            break;
        case BRACE:
            break;
        case NUM:
            :
            break;
        default:
            break;
    }
}
